version: '3'

services:
  web:
    build: .
    command: carton exec starman -Ilib app_ibis.psgi
    volumes:
      - .:/code
      - carton:/carton
      - vendor:/vendor
    networks:
      - web
    ports:
      - "5000:5000"

networks:
  web:

volumes:
  carton:
  vendor:
